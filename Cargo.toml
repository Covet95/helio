[workspace]
members = [".", "src-tauri"]

[package]
name = "switch-api"
version = "0.2.3"
edition = "2021"
authors = ["Switch API Team"]
description = "Helio 桌面应用 API 配置管理核心库"
license = "MIT"

[lib]
name = "switch_api"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0"

# Database
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
serde_yaml = "0.9"

# Time
chrono = { version = "0.4", features = ["serde"] }

# File operations
tempfile = "3.10"
dirs = "5.0"

# Logging
tracing = "0.1"

# HTTP (probe / failover)
reqwest = { version = "0.12", features = ["json"] }

# Utilities
uuid = { version = "1.10", features = ["v4", "serde"] }

# Archive (skills backup)
tar = "0.4"
flate2 = "1.0"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.10"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

[features]
tauri-gui = []

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
