.DS_Store
target/
gui/dist/
gui/node_modules/
*.db
*.sqlite
*.backup
.switch-api/

# 切换事务 journal：含配置前镜像与 shared_config，可能有明文 API key
*.switch-journal.json
.switch-journal.json

# 开发过程产物（brainstorm / 计划），不纳入版本库
.planning/
.superpowers/

.claude/settings.local.json

# Windows 保留名：在 Git Bash 里执行 `cmd 2>nul` 会误建同名文件
nul
